const jsonStringify = [
  {
    label: 'Input',
    key: 'input',
    type: 'string',
    required: true,
    description: 'The input to be converted to a JSON string.',
    variables: true,
  },
];

export default jsonStringify;
